---
name: aetherscan-repo-context
description: Pointer to the canonical Aetherscan repo-context skill. Deep-dive context for working inside the Aetherscan repo — Breakthrough Listen's deep-learning SETI pipeline (Beta-VAE → Random Forest, multi-GPU). Read `.claude/skills/aetherscan-repo-context/SKILL.md` for the full text.
---

# Working in the Aetherscan Repo

This skill has **one** canonical copy: [`.claude/skills/aetherscan-repo-context/SKILL.md`](../../../.claude/skills/aetherscan-repo-context/SKILL.md). Read it there — install paths, config/CLI system, architecture patterns, project structure, full workflow & security.

The lean, always-on companion is [`CLAUDE.md`](../../../CLAUDE.md) at the repo root ([`AGENTS.md`](../../../AGENTS.md) points at it).

Kept as a pointer rather than a duplicate: a hand-maintained second copy drifts from the first (it already had, within a single commit), and the workflow section states facts about *this* repo's automation — the `claude.yml` handle trigger, the `claude/` branch prefix, `claude-code-review.yml` — that a name-substituted copy gets wrong.
