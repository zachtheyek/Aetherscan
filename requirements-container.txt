# Pip-only extras layered into the NGC TensorFlow 25.02 base image
# (nvcr.io/nvidia/tensorflow:25.02-tf2-py3 already ships numpy, scipy, scikit-learn,
# matplotlib, pandas, h5py, psutil, pytest, and tensorflow 2.17).
# Keep versions in lockstep with environment.yml so the conda (Ampere) and
# container (Blackwell) paths run the same API surface.
astropy>=6.1
scikit-image>=0.24
joblib>=1.4
setigen
imageio>=2.34.0
umap-learn>=0.5.6
shap>=0.46.0
slack-sdk>=3.31.0
